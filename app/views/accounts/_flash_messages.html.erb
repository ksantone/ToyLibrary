<% flash.each do |name, message| %>
	<% if name == "notice" %>
		<div class="alert alert-success">
	<% elsif name == "alert" %>
		<div class="alert alert-danger">
	<% end %>
			<%= message %>
		</div>
<% end %>